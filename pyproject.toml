[project]
name = "dspy-classification"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dspy>=2.6.10",
]

[dependency-groups]
dev = [
    "ruff>=0.9.9",
]
